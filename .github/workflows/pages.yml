name: Build and deploy site to GitHub Pages

on:
  push:
    branches:
      - main
  # Build-only validation for PRs targeting main (the deploy job is skipped).
  pull_request:
    branches:
      - main
  workflow_dispatch:

# Least privilege by default; the deploy job widens this for itself below.
permissions:
  contents: read

env:
  # Fail the build if Gemfile.lock is out of sync, and verify every gem against
  # the SHA-256 checksums recorded in Gemfile.lock's CHECKSUMS section.
  BUNDLE_FROZEN: "true"

# Per-ref group so PR builds never queue behind a production deploy; superseded
# PR builds are cancelled, but a running push/deploy on main is never cancelled.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: docs
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Ruby
        uses: ruby/setup-ruby@95ef2b042f9d7a56d8268cba8559e2842e2ad01b # v1.321.0
        with:
          ruby-version: "3.4"
          bundler-cache: true
          working-directory: docs

      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d # v6.0.0

      - name: Build with Jekyll
        run: bundle exec jekyll build --baseurl "${{ steps.pages.outputs.base_path }}"
        env:
          JEKYLL_ENV: production

      - name: Upload artifact
        uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: docs/_site

  deploy:
    needs: build
    # Skip on pull_request events — those runs only validate the build.
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      pages: write      # deploy to GitHub Pages
      id-token: write    # OIDC token for actions/deploy-pages
    # Serialize actual deployments (GitHub Pages allows only one at a time).
    concurrency:
      group: pages
      cancel-in-progress: false
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@368f82528645a54fb793d4d04e342629a3f51346 # v5.0.1
