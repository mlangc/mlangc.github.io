_site
.sass-cache
.jekyll-cache
.jekyll-metadata
vendor

# Gemfile.lock is intentionally committed (see below) so the GitHub Pages
# Actions build resolves the same gem versions that were tested locally.
!Gemfile.lock
